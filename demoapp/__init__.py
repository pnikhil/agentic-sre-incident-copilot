"""demoapp, a small checkout-api that Aegis observes.

It can inject deterministic faults and emit telemetry in the same shape that the
Milestone 1 pipeline consumes, so that a fault toggle produces an incident the
copilot can diagnose.
"""
